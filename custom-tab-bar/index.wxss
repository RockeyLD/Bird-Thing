.tab-bar {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  height: calc(80rpx + env(safe-area-inset-bottom));
  background: #FFFFFF;
  display: flex;
  justify-content: space-around;
  align-items: center;
  border-top: 1rpx solid rgba(0,0,0,0.06);
  z-index: 999;
}

.guide-overlay {
  position: absolute;
  top: -2rpx;
  left: -2rpx;
  right: -2rpx;
  bottom: -2rpx;
  background: rgba(0, 0, 0, 0.75);
  z-index: 1000;
}

.tab-item {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  flex: 1;
  height: 80rpx;
  gap: 6rpx;
}

.tab-item:first-child .tab-icon { width: 72rpx; height: 72rpx; }
.tab-icon { width: 52rpx; height: 52rpx; opacity: 0.6; transition: opacity 0.2s; }
.tab-text { font-size: 22rpx; color: #8A9A90; transition: color 0.2s; }

.tab-item.active .tab-icon { opacity: 1; }
.tab-item.active .tab-text { color: #4CAF82; font-weight: 600; }
