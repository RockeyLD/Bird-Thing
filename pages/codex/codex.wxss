.page { display: flex; flex-direction: column; height: 100vh; }

.nav-bar { padding: 120rpx 30rpx 20rpx; background: var(--bg); display: flex; justify-content: space-between; align-items: center; }
.nav-title { font-size: 40rpx; font-weight: 700; }

.filter-bar {
  display: flex;
  gap: 16rpx;
  padding: 0 30rpx 20rpx;
  background: var(--bg);
}

.filter-item {
  padding: 10rpx 28rpx;
  border-radius: 28rpx;
  font-size: 26rpx;
  background: var(--card);
  color: var(--text-secondary);
  box-shadow: 0 2rpx 8rpx rgba(0,0,0,0.04);
}

.filter-item.active {
  background: var(--primary);
  color: #fff;
  font-weight: 600;
}

.scrollarea { flex: 1; overflow-y: auto; }

.codex-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 20rpx;
}

.codex-item {
  background: var(--card);
  border-radius: 24rpx;
  padding: 24rpx 16rpx;
  text-align: center;
  box-shadow: 0 4rpx 16rpx rgba(0,0,0,0.04);
  transition: transform 0.2s;
}

.codex-item.mastered { border: 2rpx solid var(--primary); }
.codex-item.learning { border: 2rpx solid var(--secondary); }
.codex-item.locked { opacity: 0.5; }

.codex-emoji { font-size: 60rpx; margin-bottom: 8rpx; }
.codex-name { font-size: 26rpx; font-weight: 600; margin-bottom: 8rpx; }

.codex-progress { display: flex; align-items: center; gap: 8rpx; }

.progress-mini {
  flex: 1;
  height: 8rpx;
  background: rgba(0,0,0,0.06);
  border-radius: 4rpx;
  overflow: hidden;
}

.progress-mini-fill {
  height: 100%;
  background: var(--primary);
  border-radius: 4rpx;
}

.progress-text { font-size: 20rpx; color: var(--text-secondary); }

.stats-card {
  background: var(--card);
  border-radius: 24rpx;
  padding: 30rpx;
  margin-top: 30rpx;
  box-shadow: 0 4rpx 16rpx rgba(0,0,0,0.04);
}

.stats-title { font-size: 30rpx; font-weight: 700; margin-bottom: 20rpx; }

.stats-row { display: flex; justify-content: space-around; }

.stats-block { text-align: center; }

.stats-num { font-size: 44rpx; font-weight: 700; color: var(--primary); }
.stats-label { font-size: 24rpx; color: var(--text-secondary); margin-top: 6rpx; }
